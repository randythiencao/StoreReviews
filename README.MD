### save an object into sessionStorage
    `sessionStorage.setItem('currentUser', JSON.stringify(retUser));`
### get an object from sessionStorage
    `JSON.parse(sessionStorage.getItem('currentUser'));`
